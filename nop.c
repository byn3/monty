#include "monty.h"

/**
* nop - lol this does nothing. why would we need this??
* @stack: doesnt matter
* @lineCount : insert emo quote. why even life?
*
* Return: NOTHING
*/
void nop(stack_t **stack, unsigned int lineCount)
{
	(void) stack;
	(void) lineCount;
}
